name: Pull Request Security Scans

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # 1. Gitleaks - 하드코딩된 시크릿 탐지
  secret-scan:
    name: Gitleaks Secret Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0 # 전체 히스토리 스캔

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # 2. Trivy IaC - Terraform 설정 오류 및 시크릿 스캔
  trivy-iac-scan:
    name: Trivy IaC Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Run Trivy vulnerability scanner in IaC mode
        uses: aquasecurity/trivy-action@master
        continue-on-error: true
        with:
          scan-type: 'config'
          hide-progress: false
          format: 'table'
          exit-code: '0' # 임시 soft fail: 취약점이 있어도 workflow는 계속 진행
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'

  # 3. Trivy SCA - 라이브러리/의존성 패키지 취약점 스캔
  trivy-sca-scan:
    name: Trivy SCA Scan (Node.js)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Run Trivy vulnerability scanner in fs mode
        uses: aquasecurity/trivy-action@master
        continue-on-error: true
        with:
          scan-type: 'fs'
          scan-ref: './services'
          hide-progress: false
          format: 'table'
          exit-code: '0' # 임시 soft fail: 취약점이 있어도 workflow는 계속 진행
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'

  # 4. Checkov - Terraform 심화 설정 오류 탐지
  checkov-scan:
    name: Checkov IaC Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Run Checkov action
        id: checkov
        uses: bridgecrewio/checkov-action@master
        continue-on-error: true
        with:
          directory: terraform/
          framework: terraform
          external_checks_dirs: security/checkov/custom_policies
          output_format: cli
          soft_fail: true # 임시 soft fail: 정책 위반이 있어도 workflow는 계속 진행
